public class SynceContactsScheduled implements Schedulable {
    
    public void execute (SchedulableContext sc) {
        Database.executeBatch(new SynceContactsBatch());
        Database.executeBatch(new SynceContactsBatchIterable());
    }

}
